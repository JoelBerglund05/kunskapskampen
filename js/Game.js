import startingTime from "js/Timer.js"

export default class Game {
    constructor() {
        let yourScore = 0;
        yourScore = Number(yourScore);
        let opponantScore = 0;
        opponantScore = Number(opponantScore);
    }

}